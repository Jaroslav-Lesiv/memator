import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  icon: {
    textAlign: 'left',
  },
  revert: {
    transform: [{ rotateY: '180deg' }],
    textAlign: 'right',
  },
});
