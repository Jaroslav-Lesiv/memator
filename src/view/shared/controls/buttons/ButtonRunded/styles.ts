import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    borderRadius: 30,
    minWidth: 60,
    height: 60,
    // borderWidth: 2,
    justifyContent: 'center',
    alignItems: 'center'
  },
});
